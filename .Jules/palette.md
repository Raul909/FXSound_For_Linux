## 2026-05-01 - Add keyboard navigation and ARIA roles to custom sliders
**Learning:** Custom `<div onMouseDown>` sliders require explicit ARIA roles (`role="slider"`) and keyboard event handlers (`onKeyDown`) to be accessible to screen readers and keyboard users.
**Action:** Ensure all interactive elements have appropriate ARIA attributes and keyboard support.
